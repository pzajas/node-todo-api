// import jwt from 'jsonwebtoken'
// import { env } from 'process'

// import { type IUser } from '../../../helpers/interfaces/user/interfaces'

// export const verifyRefreshToken = async (user: IUser): Promise<boolean> => {
//   if (user !== null && jwt.verify(user.refreshToken, env.REFRESH_SECRET) !== null) { return true }
// }
