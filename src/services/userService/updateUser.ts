// import { PrismaClient } from '@prisma/client'

// import { type IUser } from '../../../helpers/interfaces/user/interfaces'

// const prisma = new PrismaClient()

// export const updateUser = async (token: string, user: IUser): Promise<any> => {
//   const createdUser = await prisma.user.update({
//     where: {
//       id: user.id
//     },
//     data: {
//       token
//     }
//   })
//   return createdUser
// }
